<%= form_for(@ignore_list) do |f| %>
  <% if @ignore_list.errors.any? %>
    <div class="error">
      <h2><%= pluralize(@ignore_list.errors.count, "error") %> prohibited this ignore_list from being saved:</h2>

      <ul>
      <% @ignore_list.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :pattern_list %><br />
    <%= f.text_area :pattern_list %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
